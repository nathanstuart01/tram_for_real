version: '3.7'

services:
  api:
    build:
      ./api
    ports: 
      - "5000:5000"
  db:
    image: mysql
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: tram
      MYSQL_USER: root
      MYSQL_ROOT_PASSWORD: password

